using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
        //transform.parent.FindChild("target").position += new Vector3(0, 0.1f, 0);
        //Debug.Log("Position : " + transform.parent.FindChild("test").position);
        //Debug.Log("Local position : " + transform.parent.FindChild("test").localPosition);
	}
}
